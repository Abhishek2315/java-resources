package com.learn;

public class Primitive {
    public static void main(String[] args) {
        int rollno = 64;
        char letter = 'r';
        float marks = 98.67f;

        // All the decimal values are there by default they are double
        // if you want to store it in float you have add f in last and double gives always correct decimal values
        double large_Decimal_Numbers = 4567654.4567;

        // All values by default integer but if we want to store large values we will use long
        long largeInteger = 34567834567876543L;
        boolean check = false;
    }
}
